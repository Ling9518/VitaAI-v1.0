# 智慧医院系统部署说明文档

**文档版本**: v1.0  
**创建日期**: 2026年5月13日  
**文档作者**: 夏保霖  

---

## 目录

1. [部署概述](#1-部署概述)
2. [环境要求](#2-环境要求)
3. [服务器规划](#3-服务器规划)
4. [Docker部署](#4-docker部署)
5. [Kubernetes部署](#5-kubernetes部署)
6. [中间件安装](#6-中间件安装)
7. [应用配置](#7-应用配置)
8. [CI/CD配置](#8-cicd配置)
9. [运维监控](#9-运维监控)
10. [备份恢复](#10-备份恢复)
11. [故障排查](#11-故障排查)

---

## 1. 部署概述

### 1.1 部署架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              用户访问层                                  │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐               │
│  │   PC Web   │    │ Mobile Web │    │    APP    │               │
│  └──────┬──────┘    └──────┬──────┘    └──────┬──────┘               │
└─────────┼───────────────────┼───────────────────┼─────────────────────┘
          │                   │                   │
          └───────────────────┼───────────────────┘
                              │
┌─────────────────────────────┼───────────────────────────────────────────┐
│                           CDN/WAF 层                                    │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │                阿里云CDN + WAF + SSL证书                         │ │
│  └─────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────┬─────────────────────────────────────┘
                                  │
┌─────────────────────────────────┼─────────────────────────────────────┐
│                           负载均衡层                                    │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐             │
│  │   Nginx-1  │    │   Nginx-2  │    │   Nginx-3  │             │
│  │  (Keepalived) │  │  (Keepalived) │  │  (Keepalived) │             │
│  └──────┬──────┘    └──────┬──────┘    └──────┬──────┘             │
└─────────┼───────────────────┼───────────────────┼─────────────────────┘
          │                   │                   │
          └───────────────────┼───────────────────┘
                              │
┌─────────────────────────────┼───────────────────────────────────────────┐
│                           网关层                                         │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │                Spring Cloud Gateway + Sentinel                   │ │
│  └─────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────┬─────────────────────────────────────┘
                                  │
┌─────────────────────────────────┼─────────────────────────────────────┐
│                           服务层（微服务集群）                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐│
│  │User Svc  │  │Disease Svc│  │ Drug Svc │  │  AI Svc  │  │FeedbkSvc││
│  │  ×3     │  │   ×3     │  │   ×3     │  │   ×3     │  │  ×2   ││
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  └────────┘│
└─────────────────────────────────┬─────────────────────────────────────┘
                                  │
┌─────────────────────────────────┼─────────────────────────────────────┐
│                           数据层                                         │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │
│  │   MySQL    │ │   Redis     │ │ Elasticsearch│ │   MinIO     │   │
│  │ Master-Slave│ │  Cluster   │ │   Cluster   │ │   集群      │   │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘   │
└───────────────────────────────────────────────────────────────────────┘
```

### 1.2 部署模式

| 部署环境 | 用途 | 部署方式 |
|----------|------|----------|
| 开发环境 | 开发调试 | Docker Compose |
| 测试环境 | 功能测试、集成测试 | Docker Compose |
| 预发布环境 | 上线前验证 | Docker Compose |
| 生产环境 | 正式运营 | Kubernetes |

### 1.3 部署流程

```
代码提交 → GitLab CI → 构建镜像 → 推送Registry → 部署测试环境 → 部署预发布 → 部署生产
                                              ↓                              ↓
                                         自动化测试                   审批确认
```

---

## 2. 环境要求

### 2.1 软件要求

| 软件 | 版本 | 说明 |
|------|------|------|
| Docker | 20.10+ | 容器引擎 |
| Docker Compose | 2.0+ | 容器编排 |
| Kubernetes | 1.25+ | 容器编排（生产环境） |
| MySQL | 8.0+ | 主数据库 |
| Redis | 7.0+ | 缓存和会话 |
| Elasticsearch | 8.x | 搜索引擎 |
| MinIO | 2023+ | 对象存储 |
| Kafka | 3.x | 消息队列（Canal同步） |
| JDK | 17+ | Java运行时 |
| Node.js | 18+ | 前端构建 |
| Nginx | 1.24+ | 反向代理 |

### 2.2 硬件要求

#### 开发/测试环境

| 组件 | CPU | 内存 | 磁盘 | 数量 |
|------|-----|------|------|------|
| 应用服务器 | 4核 | 8GB | 100GB | 1 |
| 数据库服务器 | 4核 | 8GB | 200GB | 1 |
| 中间件服务器 | 4核 | 8GB | 100GB | 1 |

#### 生产环境（单节点参考）

| 组件 | CPU | 内存 | 磁盘 | 数量 |
|------|-----|------|------|------|
| 负载均衡节点 | 4核 | 8GB | 100GB | 2 |
| 网关节点 | 8核 | 16GB | 100GB | 3 |
| 应用节点 | 8核 | 32GB | 200GB | 6 |
| 数据库主节点 | 16核 | 64GB | 500GB SSD | 1 |
| 数据库从节点 | 16核 | 64GB | 500GB SSD | 2 |
| Redis节点 | 8核 | 32GB | 200GB SSD | 3 |
| ES节点 | 8核 | 32GB | 500GB SSD | 3 |
| MinIO节点 | 8核 | 32GB | 1TB | 4 |

---

## 3. 服务器规划

### 3.1 生产环境服务器规划

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         VPC 网络 (10.0.0.0/16)                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐        │
│  │   公网子网      │  │   应用子网       │  │   数据子网       │        │
│  │ 10.0.1.0/24    │  │ 10.0.2.0/24     │  │ 10.0.3.0/24     │        │
│  ├─────────────────┤  ├─────────────────┤  ├─────────────────┤        │
│  │ • Nginx ×3     │  │ • Gateway ×3   │  │ • MySQL Master  │        │
│  │ • WAF ×2       │  │ • User Svc ×3  │  │ • MySQL Slave×2 │        │
│  │ • CDN          │  │ • Disease Svc │  │ • Redis ×3     │        │
│  │                │  │   ×3           │  │ • ES ×3        │        │
│  │                │  │ • Drug Svc ×3 │  │ • MinIO ×4     │        │
│  │                │  │ • AI Svc ×3   │  │ • Kafka ×3     │        │
│  │                │  │ • FeedbackSvc │  │                │        │
│  │                │  │   ×2          │  │                │        │
│  └─────────────────┘  │ • Frontend ×2│  └─────────────────┘        │
│                        └─────────────────┘                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.2 端口规划

| 服务 | 端口 | 协议 | 说明 |
|------|------|------|------|
| Nginx | 80, 443 | HTTP/HTTPS | Web入口 |
| Spring Gateway | 8080 | HTTP | API网关 |
| User Service | 8081 | HTTP | 用户服务 |
| Disease Service | 8082 | HTTP | 疾病服务 |
| Drug Service | 8083 | HTTP | 药品服务 |
| AI Service | 8084 | HTTP | AI服务 |
| Feedback Service | 8085 | HTTP | 反馈服务 |
| Review Service | 8086 | HTTP | 审核服务 |
| MySQL | 3306 | TCP | 数据库 |
| Redis | 6379 | TCP | 缓存 |
| Elasticsearch | 9200 | HTTP | 搜索 |
| MinIO API | 9000 | HTTP | 对象存储 |
| MinIO Console | 9001 | HTTP | 管理界面 |
| Kafka | 9092 | TCP | 消息队列 |

---

## 4. Docker部署

### 4.1 Docker Compose配置

#### 目录结构

```
smart-hospital/
├── docker/
│   ├── docker-compose.yml
│   ├── .env
│   ├── mysql/
│   │   ├── master/
│   │   │   └── init.sql
│   │   └── slave/
│   ├── redis/
│   │   └── redis.conf
│   ├── elasticsearch/
│   │   └── elasticsearch.yml
│   ├── minio/
│   │   └── config.json
│   ├── nginx/
│   │   ├── nginx.conf
│   │   └── conf.d/
│   └── app/
│       ├── backend/
│       │   └── Dockerfile
│       └── frontend/
│           └── Dockerfile
└── config/
    ├── application.yml
    └── application-prod.yml
```

#### 环境变量文件

```bash
# .env
# 基础配置
PROJECT_NAME=smart-hospital
COMPOSE_PROJECT_NAME=smart-hospital
TIMEZONE=Asia/Shanghai

# MySQL配置
MYSQL_ROOT_PASSWORD=RootPassword123!
MYSQL_DATABASE=smart_hospital
MYSQL_MASTER_PORT=3306
MYSQL_SLAVE_PORT=3307

# Redis配置
REDIS_PASSWORD=RedisPassword123!

# Elasticsearch配置
ES_JAVA_OPTS=-Xms2g -Xmx2g
ES_HEAP_SIZE=2g

# MinIO配置
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=MinioPassword123!

# JWT配置
JWT_SECRET=your-256-bit-secret-key-here-change-in-production
JWT_EXPIRATION=7200000

# AI服务配置
AI_API_KEY=your-ai-api-key
AI_BASE_URL=https://api.openai.com

# 邮件配置
MAIL_HOST=smtp.example.com
MAIL_USERNAME=noreply@smarthospital.com
MAIL_PASSWORD=mailpassword

# 文件存储配置
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=MinioPassword123!
```

#### Docker Compose主文件

```yaml
# docker-compose.yml
version: '3.8'

services:
  # ============ MySQL Master ============
  mysql-master:
    image: mysql:8.0
    container_name: mysql-master
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      TZ: ${TIMEZONE}
    ports:
      - "${MYSQL_MASTER_PORT}:3306"
    volumes:
      - mysql_master_data:/var/lib/mysql
      - ./docker/mysql/master/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./docker/mysql/master/my.cnf:/etc/mysql/conf.d/my.cnf
    command:
      - --server-id=1
      - --log-bin=mysql-bin
      - --binlog-format=ROW
      - --gtid-mode=ON
      - --enforce-gtid-consistency=ON
    networks:
      - backend
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============ MySQL Slave ============
  mysql-slave:
    image: mysql:8.0
    container_name: mysql-slave
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      TZ: ${TIMEZONE}
    ports:
      - "${MYSQL_SLAVE_PORT}:3306"
    volumes:
      - mysql_slave_data:/var/lib/mysql
      - ./docker/mysql/slave/my.cnf:/etc/mysql/conf.d/my.cnf
    command:
      - --server-id=2
      - --relay-log=relay-bin
      - --read-only=ON
      - --log-bin=mysql-bin
      - --binlog-format=ROW
      - --gtid-mode=ON
      - --enforce-gtid-consistency=ON
    depends_on:
      mysql-master:
        condition: service_healthy
    networks:
      - backend
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============ Redis Cluster ============
  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD} --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
      - ./docker/redis/redis.conf:/usr/local/etc/redis/redis.conf
    networks:
      - backend
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============ Elasticsearch ============
  elasticsearch:
    image: elasticsearch:8.11.0
    container_name: elasticsearch
    restart: unless-stopped
    environment:
      - discovery.type=single-node
      - ES_JAVA_OPTS=${ES_JAVA_OPTS}
      - xpack.security.enabled=false
      - xpack.security.http.ssl.enabled=false
      - "ES_JAVA_OPTS=-Xms2g -Xmx2g"
    ports:
      - "9200:9200"
      - "9300:9300"
    volumes:
      - es_data:/usr/share/elasticsearch/data
      - ./docker/elasticsearch/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9200/_cluster/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5

  # ============ MinIO ============
  minio:
    image: minio/minio:latest
    container_name: minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    networks:
      - backend
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  # ============ Kafka ============
  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: kafka
    restart: unless-stopped
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
    ports:
      - "9092:9092"
    volumes:
      - kafka_data:/var/lib/kafka/data
    depends_on:
      - zookeeper
    networks:
      - backend

  # ============ Zookeeper ============
  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: zookeeper
    restart: unless-stopped
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"
    volumes:
      - zookeeper_data:/var/lib/zookeeper/data
    networks:
      - backend

  # ============ Canal ============
  canal:
    image: canal/canal-server:v1.1.6
    container_name: canal
    restart: unless-stopped
    environment:
      canal.server.mode: mysql
      canal.mysql.ds.database: ${MYSQL_DATABASE}
    ports:
      - "11110:11110"
      - "11111:11111"
    volumes:
      - ./docker/canal/:/home/admin/canal/conf/
    depends_on:
      mysql-master:
        condition: service_healthy
    networks:
      - backend

  # ============ 后端服务 ============
  backend:
    build:
      context: .
      dockerfile: docker/app/backend/Dockerfile
    container_name: smart-hospital-backend
    restart: unless-stopped
    environment:
      SPRING_PROFILES_ACTIVE: prod
      MYSQL_HOST: mysql-master
      MYSQL_PORT: 3306
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USERNAME: root
      MYSQL_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      ES_HOSTS: elasticsearch:9200
      MINIO_ENDPOINT: ${MINIO_ENDPOINT}
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY}
      JWT_SECRET: ${JWT_SECRET}
      AI_API_KEY: ${AI_API_KEY}
      MAIL_HOST: ${MAIL_HOST}
      MAIL_USERNAME: ${MAIL_USERNAME}
      MAIL_PASSWORD: ${MAIL_PASSWORD}
    ports:
      - "8080:8080"
    depends_on:
      mysql-master:
        condition: service_healthy
      redis:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - backend

  # ============ 前端服务 ============
  frontend:
    build:
      context: .
      dockerfile: docker/app/frontend/Dockerfile
    container_name: smart-hospital-frontend
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./docker/nginx/conf.d:/etc/nginx/conf.d
      - ./docker/nginx/ssl:/etc/nginx/ssl
    depends_on:
      - backend
    networks:
      - backend

# ============ 网络配置 ============
networks:
  backend:
    driver: bridge

# ============ 存储卷 ============
volumes:
  mysql_master_data:
  mysql_slave_data:
  redis_data:
  es_data:
  minio_data:
  kafka_data:
  zookeeper_data:
```

### 4.2 MySQL主从配置

#### Master配置文件

```ini
# docker/mysql/master/my.cnf
[mysqld]
server-id = 1
log-bin = mysql-bin
binlog-format = ROW
gtid-mode = ON
enforce-gtid-consistency = ON
binlog-row-image = FULL
max-binlog-size = 100M
binlog-expire-logs-seconds = 604800
sync-binlog = 1
innodb_flush_log_at_trx_commit = 1

# 从库连接配置
log-slave-updates = ON

# 字符集
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

# 性能优化
innodb-buffer-pool-size = 1G
innodb-log-file-size = 256M
max-connections = 500
```

### 4.3 后端Dockerfile

```dockerfile
# docker/app/backend/Dockerfile
FROM maven:3.9-eclipse-temurin-17 AS builder

WORKDIR /app

# 复制pom.xml
COPY backend/pom.xml .
COPY backend/src ./src

# 设置镜像源
RUN sed -i 's|https://repo.maven.apache.org/maven2|https://maven.aliyun.com/repository/public|g' /etc/maven/settings.xml

# 构建项目
RUN mvn clean package -DskipTests

# 运行阶段
FROM eclipse-temurin:17-jre

WORKDIR /app

# 复制jar包
COPY --from=builder /app/target/*.jar app.jar

# 复制配置文件
COPY docker/app/backend/logback-spring.xml /app/logback-spring.xml

# 环境变量
ENV JAVA_OPTS="-Xms512m -Xmx1024m -XX:+UseG1GC"

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=3 \
  CMD curl -f http://localhost:8080/actuator/health || exit 1

EXPOSE 8080

ENTRYPOINT ["sh", "-c", "java $JAVA_OPTS -jar app.jar"]
```

### 4.4 前端Dockerfile

```dockerfile
# docker/app/frontend/Dockerfile
FROM node:18-alpine AS builder

WORKDIR /app

# 复制package.json
COPY frontend/package.json frontend/package-lock.json* ./

# 安装依赖
RUN npm install --registry=https://registry.npmmirror.com

# 复制源代码
COPY frontend/ ./

# 构建项目
RUN npm run build

# 运行阶段
FROM nginx:alpine

# 复制构建产物
COPY --from=builder /app/dist /usr/share/nginx/html

# 复制nginx配置
COPY docker/nginx/nginx.conf /etc/nginx/nginx.conf
COPY docker/nginx/conf.d /etc/nginx/conf.d

# 复制SSL证书（可选）
COPY docker/nginx/ssl /etc/nginx/ssl

EXPOSE 80 443

CMD ["nginx", "-g", "daemon off;"]
```

### 4.5 部署执行

```bash
# 1. 克隆代码
git clone https://github.com/your-username/smart-hospital-system.git
cd smart-hospital-system

# 2. 配置环境变量
cp docker/.env.example docker/.env
# 编辑.env文件，填入实际配置

# 3. 启动所有服务
docker-compose up -d

# 4. 查看服务状态
docker-compose ps

# 5. 查看日志
docker-compose logs -f backend

# 6. 初始化数据库（首次）
docker-compose exec mysql-master mysql -uroot -p${MYSQL_ROOT_PASSWORD} < docker/mysql/master/init.sql
```

---

## 5. Kubernetes部署

### 5.1 Helm Chart结构

```
smart-hospital/
├── Chart.yaml
├── values.yaml
├── values-prod.yaml
├── templates/
│   ├── _helpers.tpl
│   ├── namespace.yaml
│   ├── configmap.yaml
│   ├── secret.yaml
│   ├── mysql/
│   │   ├── statefulset.yaml
│   │   └── service.yaml
│   ├── redis/
│   │   ├── deployment.yaml
│   │   └── service.yaml
│   ├── elasticsearch/
│   │   └── ...
│   ├── backend/
│   │   ├── deployment.yaml
│   │   ├── service.yaml
│   │   ├── hpa.yaml
│   │   └── ingress.yaml
│   └── frontend/
│       └── ...
```

### 5.2 values.yaml

```yaml
# values.yaml
namespace: smart-hospital

# 镜像配置
image:
  backend: registry.example.com/smart-hospital/backend:latest
  frontend: registry.example.com/smart-hospital/frontend:latest
  pullPolicy: IfNotPresent

# 后端服务配置
backend:
  replicaCount: 3
  resources:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      cpu: 2000m
      memory: 2Gi
  jvmOpts: "-Xms1g -Xmx2g -XX:+UseG1GC"
  autoscaling:
    enabled: true
    minReplicas: 3
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80

# 数据库配置
mysql:
  enabled: true
  architecture: replication
  auth:
    database: smart_hospital
    username: smarthospital
    password: mysqlpassword
    rootPassword: rootpassword
  primary:
    persistence:
      enabled: true
      size: 50Gi
      storageClass: ssd
    resources:
      requests:
        cpu: 1
        memory: 2Gi
      limits:
        cpu: 2
        memory: 4Gi
  secondary:
    replicaCount: 2
    persistence:
      enabled: true
      size: 50Gi

# Redis配置
redis:
  enabled: true
  architecture: replication
  auth:
    enabled: true
    password: redispassword
  master:
    persistence:
      enabled: true
      size: 10Gi
    resources:
      requests:
        cpu: 500m
        memory: 1Gi
      limits:
        cpu: 1000m
        memory: 2Gi

# Elasticsearch配置
elasticsearch:
  enabled: true
  replicas: 3
  resources:
    requests:
      cpu: 1
      memory: 2Gi
    limits:
      cpu: 2000m
      memory: 4Gi
  persistence:
    enabled: true
    size: 100Gi
  esJavaOpts: "-Xms2g -Xmx2g"

# MinIO配置
minio:
  enabled: true
  persistence:
    enabled: true
    size: 200Gi
  resources:
    requests:
      cpu: 500m
      memory: 1Gi

# Ingress配置
ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
  hosts:
    - host: api.smarthospital.com
      paths:
        - path: /
          pathType: Prefix
          service: backend
          port: 8080
    - host: www.smarthospital.com
      paths:
        - path: /
          pathType: Prefix
          service: frontend
          port: 80
  tls:
    - secretName: smart-hospital-tls
      hosts:
        - api.smarthospital.com
        - www.smarthospital.com

# 监控配置
monitoring:
  enabled: true
  prometheus:
    enabled: true
  grafana:
    enabled: true
  alertmanager:
    enabled: true
```

### 5.3 部署命令

```bash
# 1. 添加Helm仓库
helm repo add bitnami https://charts.bitnami.com/bitnami
helm repo update

# 2. 创建命名空间
kubectl create namespace smart-hospital

# 3. 创建Secret
kubectl create secret generic smart-hospital-secrets \
  --from-literal=mysql-root-password=rootpassword \
  --from-literal=mysql-password=mysqlpassword \
  --from-literal=redis-password=redispassword \
  --from-literal=jwt-secret=jwtsecretkey123456 \
  --from-literal=ai-api-key=youraiapikey \
  --namespace=smart-hospital

# 4. 部署
helm upgrade --install smart-hospital ./smart-hospital \
  --namespace smart-hospital \
  --values ./values-prod.yaml \
  --timeout 30m \
  --wait

# 5. 查看部署状态
kubectl get pods -n smart-hospital
kubectl get svc -n smart-hospital
kubectl get ingress -n smart-hospital

# 6. 查看日志
kubectl logs -f deployment/smart-hospital-backend -n smart-hospital
```

---

## 6. 中间件安装

### 6.1 MySQL主从配置

```bash
# 在Master上执行，创建复制账户
CREATE USER 'repl'@'%' IDENTIFIED BY 'replpassword';
GRANT REPLICATION SLAVE ON *.* TO 'repl'@'%';
FLUSH PRIVILEGES;

# 在Slave上执行，配置主从复制
CHANGE MASTER TO
  MASTER_HOST='mysql-master',
  MASTER_USER='repl',
  MASTER_PASSWORD='replpassword',
  MASTER_PORT=3306,
  MASTER_AUTO_POSITION=1;

START SLAVE;
SHOW SLAVE STATUS\G;
```

### 6.2 Elasticsearch索引创建

```bash
# 创建疾病索引
curl -X PUT "http://elasticsearch:9200/diseases" -H "Content-Type: application/json" -d @diseases_index.json

# 创建药品索引
curl -X PUT "http://elasticsearch:9200/drugs" -H "Content-Type: application/json" -d @drugs_index.json

# 创建技能索引
curl -X PUT "http://elasticsearch:9200/skills" -H "Content-Type: application/json" -d @skills_index.json
```

### 6.3 MinIO桶创建

```bash
# 创建桶
mc alias set myminio http://minio:9000 minioadmin MinioPassword123!
mc mb myminio/smart-hospital-reports
mc mb myminio/smart-hospital-avatars
mc mb myminio/smart-hospital-images

# 设置桶策略
mc anonymous set download myminio/smart-hospital-reports
mc anonymous set download myminio/smart-hospital-avatars
mc anonymous set upload myminio/smart-hospital-images
```

---

## 7. 应用配置

### 7.1 application.yml

```yaml
# application.yml
spring:
  application:
    name: smart-hospital
  
  profiles:
    active: prod
  
  # 数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 10
      maximum-pool-size: 50
      idle-timeout: 300000
      max-lifetime: 1800000
      connection-timeout: 30000
  
  # Redis配置
  redis:
    timeout: 5000ms
    lettuce:
      pool:
        max-active: 50
        max-idle: 20
        min-idle: 5
  
  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

# JWT配置
jwt:
  secret: ${JWT_SECRET}
  expiration: 7200000
  refresh-expiration: 604800000

# AI服务配置
ai:
  api-key: ${AI_API_KEY}
  base-url: ${AI_BASE_URL}
  model: gpt-4
  temperature: 0.7
  max-tokens: 2000

# MinIO配置
minio:
  endpoint: ${MINIO_ENDPOINT}
  access-key: ${MINIO_ACCESS_KEY}
  secret-key: ${MINIO_SECRET_KEY}
  bucket:
    reports: smart-hospital-reports
    avatars: smart-hospital-avatars
    images: smart-hospital-images

# 审计日志配置
audit:
  enabled: true
  async-enabled: true
  retention-days: 180
```

### 7.2 Nginx配置

```nginx
# docker/nginx/nginx.conf
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 10240;
    use epoll;
    multi_accept on;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 50m;

    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript 
               application/json application/javascript application/xml+rss;

    include /etc/nginx/conf.d/*.conf;
}
```

---

## 8. CI/CD配置

### 8.1 GitLab CI配置

```yaml
# .gitlab-ci.yml
stages:
  - build
  - test
  - push
  - deploy

variables:
  DOCKER_REGISTRY: registry.example.com
  DOCKER_IMAGE_BACKEND: $DOCKER_REGISTRY/smart-hospital/backend
  DOCKER_IMAGE_FRONTEND: $DOCKER_REGISTRY/smart-hospital/frontend

# 构建后端
build:backend:
  stage: build
  image: maven:3.9-eclipse-temurin-17
  script:
    - cd backend
    - mvn clean package -DskipTests
    - docker build -t $DOCKER_IMAGE_BACKEND:$CI_COMMIT_SHA -f Dockerfile .
  artifacts:
    paths:
      - backend/target/*.jar
  only:
    - main
    - develop

# 构建前端
build:frontend:
  stage: build
  image: node:18-alpine
  script:
    - cd frontend
    - npm install
    - npm run build
  artifacts:
    paths:
      - frontend/dist
  only:
    - main
    - develop

# 单元测试
test:unit:
  stage: test
  image: maven:3.9-eclipse-temurin-17
  script:
    - mvn test
  coverage: '/Total:.*?([0-9]{1,3})%/'

# 推送镜像
push:backend:
  stage: push
  script:
    - docker login $DOCKER_REGISTRY -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD
    - docker push $DOCKER_IMAGE_BACKEND:$CI_COMMIT_SHA
    - docker tag $DOCKER_IMAGE_BACKEND:$CI_COMMIT_SHA $DOCKER_IMAGE_BACKEND:latest
    - docker push $DOCKER_IMAGE_BACKEND:latest
  only:
    - main

# 部署测试环境
deploy:test:
  stage: deploy
  script:
    - kubectl config use-context test
    - kubectl set image deployment/smart-hospital-backend backend=$DOCKER_IMAGE_BACKEND:$CI_COMMIT_SHA
    - kubectl rollout status deployment/smart-hospital-backend
  environment:
    name: test
    url: https://test.smarthospital.com
  only:
    - develop

# 部署生产环境
deploy:prod:
  stage: deploy
  script:
    - kubectl config use-context prod
    - kubectl set image deployment/smart-hospital-backend backend=$DOCKER_IMAGE_BACKEND:$CI_COMMIT_SHA
    - kubectl set image deployment/smart-hospital-frontend frontend=$DOCKER_IMAGE_FRONTEND:$CI_COMMIT_SHA
    - kubectl rollout status deployment/smart-hospital-backend
    - kubectl rollout status deployment/smart-hospital-frontend
  environment:
    name: production
    url: https://www.smarthospital.com
  when: manual
  only:
    - main
```

---

## 9. 运维监控

### 9.1 Prometheus配置

```yaml
# prometheus.yml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets:
          - alertmanager:9093

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  # Spring Boot Actuator
  - job_name: 'smart-hospital'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['backend:8080']
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance

  # MySQL
  - job_name: 'mysql'
    static_configs:
      - targets: ['mysql-exporter:9104']

  # Redis
  - job_name: 'redis'
    static_configs:
      - targets: ['redis-exporter:9121']

  # Elasticsearch
  - job_name: 'elasticsearch'
    static_configs:
      - targets: ['elasticsearch:9200']
```

### 9.2 告警规则

```yaml
# rules/app-alerts.yml
groups:
  - name: smart-hospital-alerts
    rules:
      # 服务不可用
      - alert: ServiceDown
        expr: up{job="smart-hospital"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "服务 {{ $labels.instance }} 不可用"
          
      # CPU使用率高
      - alert: HighCPUUsage
        expr: rate(process_cpu_seconds_total{job="smart-hospital"}[5m]) * 100 > 80
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "服务 {{ $labels.instance }} CPU使用率过高"
          
      # 内存使用率高
      - alert: HighMemoryUsage
        expr: jvm_memory_used_bytes{job="smart-hospital", area="heap"} / jvm_memory_max_bytes{job="smart-hospital"} > 0.9
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "服务 {{ $labels.instance }} 内存使用率超过90%"
          
      # 响应时间过长
      - alert: HighResponseTime
        expr: histogram_quantile(0.95, rate(http_server_requests_seconds_bucket{job="smart-hospital"}[5m])) > 2
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "服务 {{ $labels.instance }} 响应时间过长"
          
      # 数据库连接池满
      - alert: HikariPoolExhausted
        expr: hikaricp_connections_active{pool="HikariPool-1"} / hikaricp_connections_max{pool="HikariPool-1"} > 0.95
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "数据库连接池即将耗尽"
```

---

## 10. 备份恢复

### 10.1 自动备份脚本

```bash
#!/bin/bash
# backup.sh

# 配置
BACKUP_DIR="/data/backup"
MYSQL_HOST="mysql-master"
MYSQL_USER="root"
MYSQL_PASSWORD="${MYSQL_ROOT_PASSWORD}"
DATE=$(date +%Y%m%d_%H%M%S)

# 创建备份目录
mkdir -p ${BACKUP_DIR}/{mysql,es,files}

# MySQL全量备份
echo "Starting MySQL backup..."
mysqldump -h${MYSQL_HOST} -u${MYSQL_USER} -p${MYSQL_PASSWORD} \
  --single-transaction \
  --routines \
  --triggers \
  --events \
  --master-data=2 \
  smart_hospital | gzip > ${BACKUP_DIR}/mysql/smart_hospital_${DATE}.sql.gz

# 删除7天前的备份
find ${BACKUP_DIR}/mysql -name "*.sql.gz" -mtime +7 -delete

# Elasticsearch索引备份
echo "Starting Elasticsearch backup..."
curl -X PUT "http://elasticsearch:9200/_snapshot/backup/snapshot_${DATE}?wait_for_completion=true"

# MinIO文件备份
echo "Starting MinIO backup..."
mc mirror myminio/smart-hospital-reports ${BACKUP_DIR}/files/reports_${DATE} --force

echo "Backup completed at ${DATE}"
```

### 10.2 恢复脚本

```bash
#!/bin/bash
# restore.sh

if [ -z "$1" ]; then
  echo "Usage: $0 <backup_file>"
  exit 1
fi

BACKUP_FILE=$1

# 停止应用
kubectl scale deployment smart-hospital-backend --replicas=0

# 恢复MySQL
echo "Restoring MySQL..."
gunzip < ${BACKUP_FILE} | mysql -h${MYSQL_HOST} -u${MYSQL_USER} -p${MYSQL_PASSWORD}

# 重启应用
kubectl scale deployment smart-hospital-backend --replicas=3

echo "Restore completed"
```

---

## 11. 故障排查

### 11.1 常见问题及解决方案

| 问题现象 | 可能原因 | 解决方案 |
|----------|----------|----------|
| 服务启动失败 | 端口被占用 | 检查端口占用：`netstat -tlnp \| grep <port>` |
| 数据库连接失败 | 网络不通或密码错误 | 检查网络连通性，验证密码 |
| 前端无法访问API | 跨域配置问题 | 检查Nginx和网关CORS配置 |
| 图片上传失败 | MinIO服务异常 | 检查MinIO服务状态和桶权限 |
| AI服务响应慢 | API限流或网络问题 | 检查AI服务日志，增加超时时间 |
| Elasticsearch搜索慢 | 索引碎片过多 | 执行：`POST /_forcemerge` |

### 11.2 日志查看

```bash
# 查看后端日志
kubectl logs -f deployment/smart-hospital-backend -n smart-hospital --tail=100

# 查看MySQL慢查询日志
kubectl exec -it mysql-master -n smart-hospital -- mysql -e "SHOW FULL PROCESSLIST;"

# 查看Redis状态
kubectl exec -it redis -n smart-hospital -- redis-cli -a ${REDIS_PASSWORD} INFO

# 查看ES集群状态
curl -X GET "http://elasticsearch:9200/_cluster/health?pretty"
```

### 11.3 健康检查

```bash
# 检查所有Pod状态
kubectl get pods -n smart-hospital

# 检查服务健康状态
curl -X GET "http://backend:8080/actuator/health"

# 检查数据库连接
kubectl exec -it mysql-master -n smart-hospital -- mysql -u${MYSQL_USER} -p${MYSQL_PASSWORD} -e "SELECT 1;"

# 检查Redis连接
kubectl exec -it redis -n smart-hospital -- redis-cli -a ${REDIS_PASSWORD} PING
```

---

**文档结束**
